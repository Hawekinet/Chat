name: lan_local_chat
description: Internal LAN chat with role-based access
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  file_picker: ^8.3.2

flutter:
  uses-material-design: true
